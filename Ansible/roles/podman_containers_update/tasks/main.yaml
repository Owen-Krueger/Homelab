- name: Pull new container images
  ansible.builtin.command: podman image pull {{ item.service_image }}
  loop: "{{ containers }}"

- name: Restart containers
  ansible.builtin.systemd_service:
    name: '{{ item.service_name }}'
    scope: user
    state: restarted
    daemon_reload: true
  loop: "{{ containers }}"
